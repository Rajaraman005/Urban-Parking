// Booking currently reuses ParkingRepository for quote orchestration so the
// booking feature remains thin until payment capture is added.
class BookingRepositoryImpl {
  const BookingRepositoryImpl();
}
